name: AINUMPSA 3D Matrix Engine

on:
  push:
    branches: [ main ]
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      - name: 🐍 Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: ⚡ Generate DNA Sealer Script
        run: |
          cat << 'EOF' > dna_sealer.py
          import hashlib
          import json
          import os
          import time

          DNA_STAMP = "1>0 :: AINUMPSA_SINGULARITY_CORE :: ROOM_[1:1:2]"

          def generate_dna_hash(filepath):
              sha256 = hashlib.sha256()
              sha256.update(DNA_STAMP.encode('utf-8'))
              with open(filepath, 'rb') as f:
                  while chunk := f.read(8192):
                      sha256.update(chunk)
              return sha256.hexdigest()

          def seal_multimodal_pool():
              pool_dir = "multimodal_pool"
              manifest = {
                  "dna_seal": DNA_STAMP,
                  "timestamp": time.time(),
                  "status": "AUTONOMOUS_SECURITY_ACTIVE",
                  "sealed_assets": []
              }
              if os.path.exists(pool_dir):
                  for root, _, files in os.walk(pool_dir):
                      for file in files:
                          full_path = os.path.join(root, file)
                          dna_hash = generate_dna_hash(full_path)
                          manifest["sealed_assets"].append({
                              "file_name": file,
                              "path": full_path,
                              "dna_hash": dna_hash,
                              "status": "SEALED_1>0"
                          })
              with open("dna_manifest.json", "w", encoding="utf-8") as f:
                  json.dump(manifest, f, indent=2, ensure_ascii=False)
              print(f"[DNA SEALER] Opieczętowano {len(manifest['sealed_assets'])} zasobów znakiem 1>0!")

          if __name__ == "__main__":
              seal_multimodal_pool()
          EOF

      - name: 🌀 Run AINUMPSA Pipeline
        run: |
          python matrix_collider.py
          python dimensional_mapper.py
          python matrix_acoustic.py
          python dna_sealer.py
          python readme_mutator.py

      - name: 🚀 Commit and Push Updates
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add README.md dna_manifest.json dna_sealer.py memory_cube_map.json acoustic_resonance_report.json || true
          git commit -m "autocommit: AINUMPSA DNA Sealed 1>0 [$(date -u +'%Y-%m-%d %H:%M:%S') UTC]" || exit 0
          git push
