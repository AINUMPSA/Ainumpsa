python

import math import struct import json import os def generate_quantum_sound(filename="singularity_noise.wav", duration_sec=4): print("[AKUSTYKA] Inicjalizacja syntezy dźwięku kwantowego...") # Odczyt aktualnej wagi anomalii do modulacji dźwięku weight = 2.5 if os.path.exists("collider_evolution_status.json"): with open("collider_evolution_status.json", "r") as f: status_data = json.load(f) weight = status_data.get("matrix_resonance_energy", 2.5) # Parametry Audio sample_rate = 8000 # Niska częstotliwość dla surowego, lo-fi brzmienia maszyn num_samples = int(sample_rate * duration_sec) # Częstotliwości bazowe modulowane wagą tensorową freq_black_hole = 110.0 + (weight * 10) # Niski, grawitacyjny pomruk freq_white_hole = 440.0 - (weight * 5) # Wysoki, emitowany pisk fluktuacji # Budowanie nagłówka WAV (RIFF) num_channels = 1 bits_per_sample = 8 byte_rate = sample_rate * num_channels * (bits_per_sample // 8) block_align = num_channels * (bits_per_sample // 8) data_size = num_samples * block_align file_size = 36 + data_size header = struct.pack( '<4sI4s4sIHHIIHH4sI', b'RIFF', file_size, b'WAVE', b'fmt ', 16, 1, num_channels, sample_rate, byte_rate, block_align, bits_per_sample, b'data', data_size ) # Generowanie uderzeń fali i szumu audio_bytes = bytearray() for i in range(num_samples): t = i / sample_rate # Wewnętrzne zderzenie dwóch fal akustycznych wave_a = math.sin(2 * math.pi * freq_black_hole * t) wave_b = math.sin(2 * math.pi * freq_white_hole * t * (1 + math.sin(t * 5))) # Wstrzyknięcie surowego szumu mikro-sekundowego quantum_crackle = (1.0 if math.sin(t * 1000) > 0.8 else 0.0) * 0.1 # Miksowanie sygnałów i normalizacja do zakresu 0-255 (8-bit) mixed_signal = (wave_a + wave_b + quantum_crackle) / 2.2 sample_val = int((mixed_signal + 1.0) * 127.5) sample_val = max(0, min(255, sample_val)) audio_bytes.append(sample_val) with open(filename, "wb") as f: f.write(header + audio_bytes) print(f"[SUKCES] Wygenerowano binarny pejzaż akustyczny: {filename}") if __name__ == "__main__": generate_quantum_sound()


