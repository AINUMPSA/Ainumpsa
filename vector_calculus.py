import numpy as np def _validate_coords(coord_x, coord_y, coord_z, L_shape): nx, ny, nz = L_shape[0], L_shape[1], L_shape[2] dx = coord_x if np.isscalar(coord_x) else np.asarray(coord_x) dy = coord_y if np.isscalar(coord_y) else np.asarray(coord_y) dz = coord_z if np.isscalar(coord_z) else np.asarray(coord_z) return dx, dy, dz def compute_divergence(L, coord_x, coord_y, coord_z): if L.shape[-1] != 3: raise ValueError("Ostatni wymiar L musi mieć rozmiar 3") Lx, Ly, Lz = L[..., 0], L[..., 1], L[..., 2] dx, dy, dz = _validate_coords(coord_x, coord_y, coord_z, L.shape) dLx_dx = np.gradient(Lx, dx, axis=0) dLy_dy = np.gradient(Ly, dy, axis=1) dLz_dz = np.gradient(Lz, dz, axis=2) return dLx_dx + dLy_dy + dLz_dz
